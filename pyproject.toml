[project]
name = "battista-backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "pathway[all]>=0.26.4",
    "torch==2.9.0",
    "transformers==4.49.0",
]

[tool.uv]
environments = [
    "sys_platform == 'linux'"
]
